from setuptools import setup

setup(
    name="Klappson's Dockerutils",
    version='0.1',
    description='Code I wrote for my docker-images',
    packages=[
        'klappson',
        'klappson.dockerutils',
    ],
)
